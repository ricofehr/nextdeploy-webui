  <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <button type="button" class="navbar-toggle collapsed" {{action 'toggleCollapsed'}}>
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        {{#if session.isAuthenticated}}
            {{#link-to 'users.edit' currentUser.id classNames="simplify-inverse navbar-brand"}}My Profile{{/link-to}}
        {{/if}}
        {{#bs-collapse collapsed=collapsed}}
        <ul class="nav navbar-nav navbar-toggle" {{action 'toggleCollapsed'}}>
          {{#if session.isAuthenticated}}
            {{#link-to 'vms.byuser' currentUser.id tagName='li' href=false}}<a href="">Vms</a>{{/link-to}}
            {{#if isDev}}
              {{#link-to 'projects' tagName='li' href=false}}<a href="">Projects</a>{{/link-to}}
            {{/if}}
            {{#if isLead}}
              {{#link-to 'users' tagName='li' href=false}}<a href="">Users</a>{{/link-to}}
            {{/if}}
            {{#if isAdmin}}
              {{#link-to 'brands' tagName='li' href=false}}<a href="">Brands</a>{{/link-to}}
            {{/if}}
            <li><a href="" {{action 'help'}}>?</a></li>
            {{#if isDev}}<li><a href="" {{action 'openGitlab'}}>Gitlab</a></li>{{/if}}
            <li><a href="" {{action 'invalidateSession'}} >Logout</a></li>
          {{else}}
            <li>{{#link-to 'login'}}Login{{/link-to}}</li>
          {{/if}}
        </ul>
        {{/bs-collapse}}
      </div>

      <div class="collapse navbar-collapse" id="main-menu">
        <div class="navbar-header">
          {{#link-to 'dashboard' href=false}}<a href="#" class="navbar-brand"><img src="images/logo.png" border="0" alt="NextDeploy" class="inline-block img-responsive"/></a>{{/link-to}}
        </div>

        <ul class="nav navbar-nav">
          {{#if session.isAuthenticated}}
            {{#link-to 'vms' tagName='li' href=false}}<a href="">Vms</a>{{/link-to}}

            {{#if isDev}}
              {{#link-to 'projects' tagName='li' href=false}}<a href="">Projects</a>{{/link-to}}
            {{/if}}
            {{#if isLead}}
              {{#link-to 'users' tagName='li' href=false}}<a href="">Users</a>{{/link-to}}
            {{/if}}
            {{#if isAdmin}}
              {{#link-to 'brands' tagName='li' href=false}}<a href="">Brands</a>{{/link-to}}
            {{/if}}
            {{#link-to 'users.edit' currentUser.id tagName='li' href=false}}<a href="">
            {{currentUser.email}}</a>{{/link-to}}
            <li><a href="" {{action 'help'}}>?</a></li>
          {{else}}
            <li>{{#link-to 'login'}}Login{{/link-to}}</li>
          {{/if}}
        </ul>
        {{#if session.isAuthenticated}}
        <ul class="nav navbar-nav navbar-right">
          {{#if showSearch}}
          <li class="navbar-form">
            <span class="glyphicon glyphicon-search" aria-hidden="true"/>{{input value=search classNames="form-control" placeholder="Search" size="12"}}
          </li>
          {{/if}}
          {{#if isDev}}<li><a href="/gitlab" target="_blank"><span class="glyphicon glyphicon-user"></span>Gitlab</a></li>{{/if}}
          <li><a href="" {{action 'invalidateSession'}} ><span class="glyphicon glyphicon-log-out"></span>Logout</a></li>
        </ul>

        {{/if}}
      </div>
    </div>
  </nav>

  {{application-help modalHelp=modalHelp}}
